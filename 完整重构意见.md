## 核心判断

这个项目的科学目标是合理的，但当前仓库实现的仍是一个“数据整理 + 历史组合重排 + 双目标 BO 演示”，还没有真正形成能够回答以下问题的逆向设计系统：

> 为达到指定的肿瘤杀伤和正常细胞安全性，应调整哪些合成参数；这些参数应把材料的粒径、表面化学、纯度等表征推到什么区间。

项目需要从当前的“扁平 MOBO”升级为：

> **工艺参数 → 材料表征 → 生物响应 → 安全约束下的逆向设计**

也就是以材料科学中的 Process–Structure–Property/Biology 链条为核心，而不是把所有字段一次性输入一个高斯过程。材料逆向设计研究也表明，显式建模工艺—结构—性能链条，比绕过材料结构、直接拟合工艺—性能关系更适合回答“为什么”和“应该怎样调参数”的问题。([ScienceDirect][1])

---

# 一、当前仓库做得较好的部分

仓库已经建立了几个正确的基础：

1. 使用 `material_id`、`material_parent_id`、`sample_id` 管理材料、批次和生物实验之间的关系。
2. 明确区分 `synthetic` 与 `commercial` 两条工艺路径。
3. 没有用虚构的 SHS 参数填充商业原料。
4. 保留数据来源、映射方式和重复记录审计信息。
5. 已经意识到安全性不能简单被综合评分掩盖，而应当在安全可行域中寻找 Pareto 候选。

当前四层架构“原始数据—规范化—BO—实验回写”作为数据流水线是成立的。

问题主要发生在建模层、验证层和候选生成层。

---

# 二、必须优先修复的问题

## 1. 混合细胞系但没有编码细胞系

当前代码在 Huh-7 完整样本少于 40 条时，会退回使用所有细胞系：

```python
huh7 = data[data["tumor_cell_line"].eq("Huh-7")]
model_data = huh7 if len(huh7) >= 40 else data
```

但实际模型特征中没有 `tumor_cell_line`。因此不同细胞系的响应会被当成同一个函数进行学习。

这会导致非常严重的伪关系。例如：

* 某一细胞系本身更敏感，模型却可能把差异归因于粒径或 SHS 温度；
* 某个材料只在特定细胞系做过实验，模型可能把材料身份和细胞系效应混为一体；
* 推荐结果无法解释适用于哪个细胞系。

建议：

* 第一版直接按 `workflow_branch × tumor_cell_line × normal_cell_line` 分模型；
* 样本不足时明确返回“证据不足”，不要自动合并；
* 数据充分后再引入多任务高斯过程，用细胞系作为 task，而不是普通 one-hot 特征。

同时，代码虽然读取了 `selection_cell`，但后续生成长表时没有保留正常细胞的具体身份。

`normal_cell_line` 必须成为正式字段。

---

## 2. 当前候选生成不是真正的参数优化

`build_candidates()` 实际上做的是：

* 从历史记录中提取已有材料参数；
* 从历史记录中提取已有工艺组合；
* 将二者做笛卡尔组合；
* 只测试固定的 125、250、500 ppm。

它不会生成新的 Mg/Si 比、SHS 温度、保温时间、球磨时间或粒径值。

因此当前系统只能回答：

> 历史上已有材料参数与已有工艺参数的哪些重新组合值得测试？

它不能回答：

> SHS 温度应当升高多少？Mg/Si 比应向哪个区间移动？球磨时间应如何调整？

正确做法是单独定义设计空间，例如：

```yaml
synthetic:
  continuous:
    mg_si_molar_ratio: [lower, upper]
    shs_max_temp_c: [lower, upper]
    hold_time_min: [lower, upper]
    initial_pressure_atm: [lower, upper]
    milling_time_min: [lower, upper]
    ball_to_material_ratio: [lower, upper]
    target_dls_nm: [lower, upper]

  categorical:
    protective_gas: [...]
    milling_mode: [...]
    post_treatment: [...]

commercial:
  continuous:
    milling_time_min: [lower, upper]
    ball_to_material_ratio: [lower, upper]
    pvp_mw: [lower, upper]
    material_to_pvp_ratio: [lower, upper]

  forbidden:
    - shs_max_temp_c
    - hold_time_min
    - initial_pressure_atm
```

上下界应来自：

* 设备边界；
* 研究人员认可的化学可行域；
* 历史数据支持范围；
* 必要的安全限制。

然后由 acquisition function 在连续和离散混合空间内真正搜索参数。

---

## 3. 材料表征字段被提取了，但没有进入模型链条

仓库已经提取了很多非常有价值的材料表征：

* Mg₂Si 纯度；
* 晶粒尺寸；
* DLS 粒径；
* PDI；
* Zeta 电位；
* Mg/Si 原子比；
* MgO、SiOx 比例；
* 氧化层厚度；
* 缺陷态；
* ROS；
* 凋亡率；
* IC50 和安全指数。

但是当前 `mobo_demo.py` 使用的主要是合成和工艺参数，DLS、PDI、Zeta、纯度和 XPS 表面信息没有进入 BO 模型。

这与项目“以材料为中心”的主旨相冲突。

Mg₂Si 的已报道生物机制不是简单的“材料存在就杀伤细胞”。相关研究显示，聚合物修饰的 Mg₂Si 纳米粒子在酸性肿瘤环境中释放硅烷，消耗组织及血红蛋白结合氧，并形成 SiO₂ 聚集体。这意味着反应活性很可能受到粒径、表面积、表面氧化状态、分散性和包覆状态共同控制。([Nature][2])

其他纳米材料实验也表明，粒径和表面电荷可能显著影响细胞摄取、亚细胞定位和细胞毒性，因此 DLS、PDI、Zeta 和表面化学不能只作为报告字段。([美国化学学会出版物][3])

---

## 4. 当前训练切分没有产生有效的模型验证

代码随机抽取大约 75% 的行用于训练，但剩余 25% 没有被用于评价：

```python
permutation = rng.permutation(len(model_data))
train_size = max(40, int(len(model_data) * 0.75))
train_data = model_data.iloc[permutation[:train_size]]
```

随后直接运行 BO。没有计算：

* 测试集误差；
* 排名能力；
* 不确定性覆盖率；
* 安全性分类准确率；
* Pareto 前沿恢复率。

而且同一实验的 125、250、500 ppm 会被展开成三行。随机按行切分时，同一材料、同一批次、同一实验的不同浓度极易同时进入训练集和测试集，形成信息泄漏。

生物机器学习研究已经反复证明，相关样本、重复个体或高度相似实体跨越数据分区，会显著高估模型泛化性能；小数据场景尤其严重。([Nature][4])

推荐使用三种验证：

1. `GroupKFold(group=material_parent_id)`：检查对新材料家族的泛化。
2. `GroupKFold(group=experiment_id)`：避免同一实验的不同浓度跨分区。
3. 按日期或实验轮次回放：用早期数据训练，预测后续实验结果。

最终报告至少包含：

* tumor viability MAE、RMSE；
* normal viability MAE、RMSE；
* Spearman 排名相关；
* 80% 和 95% 预测区间覆盖率；
* 安全可行性 Brier score；
* Top-k 安全有效候选命中率；
* 历史 BO replay 的 hypervolume regret。

模型通过这些验证后，才能进入真实主动学习。

---

## 5. 安全性不应只是第二个平等目标

目前模型同时最大化：

* 肿瘤杀伤率；
* 正常细胞存活率。

这在探索 Pareto 前沿时是可以的，但如果项目已经有明确的安全底线，更合理的形式是：

[
\min_x E[V_{\text{tumor}}(x)]
]

满足：

[
P(V_{\text{normal}}(x)\geq T_{\text{safe}})\geq 1-\alpha
]

以及：

[
P(\text{工艺可执行})\geq T_{\text{process}}
]

也就是说：

* 肿瘤效果是优化目标；
* 正常细胞安全性是概率约束；
* 稳定性和工艺可执行性也是约束；
* 只有没有明确门槛的目标才进入 Pareto 优化。

约束 Bayesian optimization 会分别建模目标和未知约束，并在可能可行的区域内选择实验。([Proceedings of Machine Learning Research][5])

如果确实需要多个效果目标，可以采用适合实验噪声的 qNEHVI，而不是普通 EHVI。qNEHVI 会对带噪声的 Pareto 前沿不确定性进行积分，更适合细胞实验和材料重复批次。([NeurIPS 会议论文集][6])

---

## 6. 当前选择性指数不稳定

代码计算：

[
\text{selectivity}
==================

\frac{100-V_{\text{tumor}}}
{100-V_{\text{normal}}}
]

当正常细胞存活率接近 100% 时，分母趋近于零，选择性指数可能因为非常小的测量误差变成极大值。

建议：

* 不把该比值作为主优化目标；
* 主模型直接使用 tumor viability 和 normal viability；
* 描述性指标使用
  [
  \Delta V=V_{\text{normal}}-V_{\text{tumor}}
  ]
* 有足够剂量点时，使用
  [
  SI = IC50_{\text{normal}}/IC50_{\text{tumor}}
  ]
* 最终决策仍使用“效果目标 + 安全约束”。

---

## 7. 剂量数据应按剂量—响应关系建模

125、250、500 ppm 是同一材料响应曲线上的三个观测，而不是三个完全独立样本。

只有三个浓度时，不建议为每个材料单独拟合四参数 Hill 曲线，参数通常不可识别。更适合的是：

* 层级 Bayesian 剂量—响应模型；
* 单调 Gaussian process；
* 在材料之间共享剂量曲线形状信息；
* 对材料特异的效力和最大响应进行部分池化。

层级剂量—响应模型可以借用不同材料和实验之间的信息，并对剂量效应和测量噪声同时给出不确定性。([OUP Academic][7])

推荐模型形式：

[
V_{i,c}
=======

g(\log c;\theta_i)
+
b_{\text{batch}}
+
\epsilon
]

其中材料参数 (\theta_i) 再由材料表征和工艺预测。

浓度应该被视为应用条件，而不是和 SHS 温度完全等价的材料设计变量。

---

## 8. 有一个高风险的 PVP 分子量解析错误

当前解析代码为：

```python
upper = str(value).upper()
if "W" in upper:
    return number * 10000
if "K" in upper:
    return number * 1000
if "M" in upper:
    return number * 1000000
```

如果原始值是：

```text
40000 g/mol
```

大写后包含 `MOL`，代码会命中 `"M"`，把 40000 再乘以 1,000,000。

而另一个解析脚本又没有 `M` 规则，导致不同流水线可能产生不同数值。

必须改成严格正则表达式，例如识别：

* `40 kDa`
* `40000 Da`
* `4万`
* `0.04 MDa`

并为每个解析规则写测试。

---

## 9. 文件发现和 Excel 解析过于脆弱

多个脚本通过：

```python
XLSX_FILES = sorted(ROOT.glob("*.xlsx"))
MG_PATH, TACE_PATH = XLSX_FILES[0], XLSX_FILES[1]
```

判断哪个文件是材料库、哪个是 TACE 数据。

只要目录内多了一个 Excel，或文件名改变，数据源就可能被交换。

应改成配置：

```yaml
sources:
  material_workbook:
    path: data/raw/material_master.xlsx
    required_sheets:
      - synthesis
      - characterization
      - surface_chemistry

  biology_workbook:
    path: data/raw/tace.xlsx
    required_sheets:
      - 数据汇总
```

程序启动时校验：

* 文件哈希；
* sheet 名称；
* 必需列；
* 单位；
* schema 版本。

---

## 10. 依赖目前不可复现

仓库指定：

```text
MultiBgolearn==0.1.1
```

截至 2026 年 7 月 26 日，PyPI 页面列出的最新 MultiBgolearn 版本是 0.1.0，并未列出 0.1.1；包状态仍标记为 Alpha。([PyPI][8])

建议：

* 不把 MultiBgolearn 作为唯一模型内核；
* 保留一个 `MultiBgolearnAdapter` 用于结果对照；
* 主模型迁移到 BoTorch/GPyTorch；
* 使用 lock 文件固定所有可安装版本；
* CI 中实际执行干净环境安装。

BoTorch 的模块化设计支持自定义概率模型、约束、并行候选和 Monte Carlo acquisition，更适合实现当前项目需要的条件空间和概率安全约束。([NeurIPS 会议论文集][9])

---

# 三、推荐的科学方法架构

## 1. 明确字段角色

不要再使用一个统一的 `feature_columns` 列表。每个字段必须属于以下角色之一。

### 可控制参数 (X)

实验人员可以在合成前设定：

* Mg/Si 摩尔比；
* Mg、Si 原料质量；
* SHS 温度；
* 保温时间；
* 真空循环和时间；
* 初始压力；
* 保护气体；
* 球磨模式和时间；
* 球料比；
* PVP 分子量；
* 材料/PVP 比；
* 超声时间；
* 后处理；
* 目标浓度。

这些字段才能作为逆向设计的决策变量。

### 材料状态 (Z)

合成完成后才能测得：

* Mg₂Si 纯度；
* 晶粒尺寸；
* DLS 粒径；
* PDI；
* Zeta 电位；
* Mg/Si 原子比；
* MgO、SiOx 比例；
* 氧化层厚度；
* 缺陷态；
* XRD/XPS 特征。

这些字段不能被优化器任意设置，而应当由第一阶段模型预测。

### 生物上下文 (C)

* 肿瘤细胞系；
* 正常细胞系；
* 浓度；
* 暴露时间；
* 培养条件；
* 实验批次和日期。

### 生物结果 (Y)

* 肿瘤细胞存活率；
* 正常细胞存活率；
* ROS；
* 凋亡率；
* IC50；
* 安全性指标。

### 干扰与追溯变量 (B)

* 供应商；
* 原料批次；
* 操作者；
* 设备；
* 测量日期；
* source sheet；
* mapping type。

这些变量用于批次校正和审计，通常不作为可调参数。

---

## 2. 建立两阶段或三阶段模型

### 第一阶段：工艺到材料表征

[
p(Z\mid X,\text{workflow branch})
]

回答：

* 改变 SHS 温度会怎样改变纯度和晶粒？
* 球磨时间和球料比会怎样改变 DLS 与 PDI？
* PVP 条件会怎样改变 Zeta 和分散稳定性？

建议先为每个表征单独训练：

* ARD Gaussian process；
* ExtraTrees/Random Forest；
* 必要时使用多输出 GP。

使用 grouped cross-validation 选择模型，而不是预先认定 GP 一定最好。实验材料领域的 BO 基准研究表明，ARD GP 和随机森林都可能表现良好，具体选择取决于数据结构。([arXiv][10])

### 第二阶段：材料表征到生物性能

[
p(Y\mid Z,C)
]

回答：

* 哪种粒径、PDI、Zeta 和表面氧化状态对应更好的杀伤—安全窗口？
* 某种材料表征在 Huh-7 上是否有效，在另一细胞系上是否仍成立？
* ROS 或凋亡是否是材料表征到杀伤性能的中间响应？

### 第三阶段：直接残差模型

[
Y=f_{Z\rightarrow Y}(Z,C)
+
f_{\text{direct}}(X,C)
+
\epsilon
]

直接残差用于吸收当前表征字段无法解释的工艺影响。

对于新候选，需要传播第一阶段的不确定性：

1. 从 (p(Z|X)) 抽样多个可能的材料表征；
2. 将每个表征样本输入 (p(Y|Z,C))；
3. 汇总得到候选生物响应分布。

不能只把第一阶段预测均值输入第二阶段，否则会低估不确定性。

---

# 四、如何回答“材料需要达到什么指标”

这里不能根据文献直接宣称：

* DLS 必须是多少 nm；
* Zeta 必须是多少 mV；
* Mg₂Si 纯度必须是多少；
* 氧化层必须多厚。

这些数值必须从你们自己的材料、细胞系和实验条件中学习。不同纳米材料、表面修饰和细胞模型之间不能直接照搬阈值。

正确输出是一个**概率材料目标域**：

[
\mathcal Z^*
============

\left{
z:
P(
V_{\text{tumor}}\leq T_{\text{effect}},
V_{\text{normal}}\geq T_{\text{safe}}
\mid z,C
)
\geq p_0
\right}
]

系统最终应输出类似下面的内容，但具体数字由数据计算：

| 材料指标       |   推荐区间 | 可信区间 | 对生物性能的作用   | 证据强度  |
| ---------- | -----: | ---: | ---------- | ----- |
| Mg₂Si 纯度   | 数据学习所得 | 后验区间 | 与肿瘤杀伤相关    | 高/中/低 |
| DLS 粒径     | 数据学习所得 | 后验区间 | 影响分散和细胞作用  | 高/中/低 |
| PDI        | 数据学习所得 | 后验区间 | 稳定性约束      | 高/中/低 |
| Zeta 电位    | 数据学习所得 | 后验区间 | 影响界面作用及安全性 | 高/中/低 |
| MgO/SiOx 比 | 数据学习所得 | 后验区间 | 表面反应状态     | 高/中/低 |
| 氧化层厚度      | 数据学习所得 | 后验区间 | 潜在反应速率中介   | 高/中/低 |

并进一步求逆：

[
X^*
===

\arg\max_X
P(Z(X)\in\mathcal Z^*)
]

这样才能输出：

> 为把 DLS、PDI 和表面化学推入目标域，建议将 Mg/Si 比向某区间移动、SHS 温度在某区间扫描、球磨时间调整到某区间。

输出应是参数区间，而不是单点处方。

---

# 五、每个推荐候选应包含的内容

建议候选结果不再只是一行 acquisition value，而是一张“实验建议卡”：

```text
candidate_id
workflow_branch
source_material_or_parent

推荐用途:
  exploitation / exploration / boundary-learning / replicate

可执行参数:
  Mg/Si ratio
  SHS temperature
  hold time
  pressure/vacuum
  milling mode/time
  ball-to-material ratio
  PVP MW and ratio
  ultrasonic time
  post-treatment

预测材料状态:
  purity: mean + interval
  grain size: mean + interval
  DLS: mean + interval
  PDI: mean + interval
  zeta: mean + interval
  surface chemistry: mean + interval

预测生物响应:
  tumor viability: mean + interval
  normal viability: mean + interval
  probability of meeting efficacy threshold
  probability of meeting safety threshold

可信性:
  distance to historical data
  out-of-domain flag
  model disagreement
  nearest historical experiments

推荐参数方向:
  parameter
  current/reference value
  proposed range
  expected change
  confidence
```

每轮实验建议至少包含四种角色：

* 安全域内的优选候选；
* 高不确定但潜在高收益的探索候选；
* 用于确认局部参数方向的邻域候选；
* 用于估计实验噪声的历史锚点重复。

---

# 六、推荐的代码架构

```text
mg2si/
├── pyproject.toml
├── uv.lock / conda-lock.yml
├── configs/
│   ├── data_sources.yaml
│   ├── schema.yaml
│   ├── design_space.yaml
│   ├── objectives.yaml
│   └── experiment.yaml
├── src/mg2si/
│   ├── cli.py
│   ├── io/
│   │   ├── excel_reader.py
│   │   ├── parsers.py
│   │   └── source_manifest.py
│   ├── schema/
│   │   ├── material.py
│   │   ├── characterization.py
│   │   ├── bioassay.py
│   │   └── candidate.py
│   ├── mapping/
│   │   ├── resolver.py
│   │   └── aliases.yaml
│   ├── data/
│   │   ├── normalize.py
│   │   ├── deduplicate.py
│   │   ├── quality.py
│   │   └── build_dataset.py
│   ├── models/
│   │   ├── process_to_state.py
│   │   ├── state_to_biology.py
│   │   ├── dose_response.py
│   │   ├── direct_baseline.py
│   │   └── uncertainty.py
│   ├── optimization/
│   │   ├── design_space.py
│   │   ├── constraints.py
│   │   ├── acquisition.py
│   │   └── recommend.py
│   ├── evaluation/
│   │   ├── grouped_splits.py
│   │   ├── calibration.py
│   │   ├── retrospective_replay.py
│   │   └── metrics.py
│   └── reporting/
│       ├── recommendation_card.py
│       └── target_profile.py
├── tests/
│   ├── fixtures/
│   │   └── synthetic_workbooks/
│   ├── test_parsers.py
│   ├── test_mapping.py
│   ├── test_schema.py
│   ├── test_group_splits.py
│   ├── test_branch_constraints.py
│   └── test_candidate_generation.py
└── docs/
    ├── DATA_DICTIONARY.md
    ├── MODEL_CARD.md
    ├── OBJECTIVE_SPEC.md
    └── EXPERIMENT_LOOP.md
```

建议提供统一 CLI：

```powershell
mg2si ingest
mg2si validate-data
mg2si build-dataset
mg2si evaluate
mg2si train
mg2si recommend
mg2si validate-candidates
mg2si replay
```

当前 `rebuild_material_cell_dataset.py`、`prepare_bo_dataset.py` 和 `export_tace_subtables.py` 包含大量重复解析逻辑，应合并成共享模块。否则同一个字段会在不同脚本中被不同方式解析。

---

# 七、数据层需要增加的工程约束

即使不能增加新的实验字段，也应为已有字段增加以下元数据：

```text
value_raw
value_numeric
unit_raw
unit_standard
range_lower
range_upper
parser_rule
parser_version
quality_flag
source_file_hash
source_sheet
source_row
```

目前将 `"100–200"` 直接转换成 150，会丢失实验范围信息。后续模型无法区分“精确测量为 150”和“只知道在 100–200 之间”。

别名也不应继续写死在 Python 代码里。当前有多个硬编码样品映射规则。

应迁移为可审计的 `aliases.yaml` 或映射表：

```yaml
aliases:
  MS-251016:
    candidates:
      - MS-251016-SHS
      - MS-251016-Q
    approved_by: researcher
    approved_date: ...
    reason: ...
```

---

# 八、验证器需要从“文件存在”升级为“科学有效”

当前 `validate_bo_contract.py` 明确只是 schema 和约束检查，不是统计测试。

它目前主要验证：

* 文件非空；
* feature 和 target 行数一致；
* branch 合法；
* 候选包含球磨和粒径字段。

建议增加：

### 数据验证

* 单位一致性；
* 重复实验识别；
* viability 是否在 0–100；
* 同一实验三浓度是否完整；
* 正常细胞身份是否存在；
* 表征是否发生在生物测试之前；
* 是否存在目标泄漏；
* 映射冲突率；
* 分支关键字段覆盖率。

### 模型验证

* grouped CV；
* 不确定性校准；
* 外推距离；
* 不同模型间一致性；
* permutation baseline；
* 时间回放。

### 推荐验证

* 所有参数在设备边界内；
* 没有不可用的类别；
* 不生成商业原料 SHS 参数；
* 没有使用未知表征作为可控制输入；
* 安全约束满足概率超过门槛；
* 推荐不是训练样本复制；
* 推荐附近有足够的数据支持，或者明确标记为探索候选。

---

# 九、推荐实施顺序

## 阶段 A：先保证数据可信

完成以下内容后再训练正式模型：

* 修复 PVP 分子量解析；
* 显式配置 Excel 文件；
* 保留正常细胞系和暴露时间；
* 保存数值范围和原始单位；
* 合并重复解析代码；
* 外置样品别名；
* 建立 synthetic fixture 和 parser tests；
* 修复不可安装的依赖版本。

## 阶段 B：建立可信基线

暂时不做复杂 PSP 模型，先实现：

* 按工艺分支分模型；
* 按细胞系分模型；
* GroupKFold；
* ARD GP、Random Forest、ExtraTrees 三类基线；
* tumor 和 normal 两个独立模型；
* 预测区间校准；
* 历史时间回放。

只有基线能够对未见材料产生可接受的排序和校准能力，才继续做 BO。

## 阶段 C：建立材料中心模型

实现：

* `process → characterization`；
* `characterization → biology`；
* 不确定性传播；
* 材料目标域；
* 工艺参数逆向求解。

## 阶段 D：进入主动学习

使用：

* 单目标 constrained BO，或带安全约束的 qNEHVI；
* 连续设计空间；
* 分支硬约束；
* 批次候选；
* 探索、利用、局部方向验证和重复锚点组合；
* 每轮实验后自动回写、重训和历史 replay。

---

# 十、建议重点阅读的论文方向

材料生物方面：

* Mg₂Si 的酸响应去氧和肿瘤饥饿机制是你们最直接的材料生物学依据；它提示项目应优先关注反应性表面、粒径、分散、氧化状态以及酸性条件响应。([Nature][2])
* 纳米粒子尺寸会改变细胞摄取动力学，表面电荷会改变摄取、细胞定位、ROS 和毒性，因此 DLS、PDI 和 Zeta 应成为材料状态核心变量。([美国化学学会出版物][3])

计算材料方面：

* 工艺—结构—性能的主动学习与逆向设计。([ScienceDirect][1])
* 带未知可制造性约束的多目标材料 BO。([ScienceDirect][11])
* 带实验噪声的 qNEHVI。([NeurIPS 会议论文集][6])
* 灵活实现自定义约束和 acquisition 的 BoTorch。([NeurIPS 会议论文集][9])
* 避免生物数据划分泄漏。([Nature][4])
* 层级、单调的剂量—响应建模。([OUP Academic][12])

---

## 最终结论

当前项目最重要的优化不是更换一个更复杂的 BO 算法，而是完成以下四项转变：

1. 从“所有字段扁平输入”转为“工艺—表征—生物响应分层建模”；
2. 从“历史组合重排”转为“受设备和化学约束的连续参数搜索”；
3. 从“随机行切分”转为“按材料、实验和时间分组的严格验证”；
4. 从“安全性作为普通目标”转为“带不确定性的安全概率约束”。

仅凭仓库和字段名称，不能诚实地给出具体的粒径、PDI、Zeta 或纯度阈值。具体指标区间应由你们的实际数据库通过上述材料目标域方法计算。

下一步最有价值的输入是脱敏后的字段字典、每列单位、缺失率、唯一值数量，以及少量结构保持一致的示例行；基于这些信息即可进一步形成可直接实施的 schema、模型配置和逐文件重构清单。

[1]: https://www.sciencedirect.com/science/article/pii/S1359645420303220?utm_source=chatgpt.com "An active learning high-throughput microstructure calibration framework for solving inverse structure–process problems in materials informatics - ScienceDirect"
[2]: https://www.nature.com/articles/nnano.2016.280 "Magnesium silicide nanoparticles as a deoxygenation agent for cancer starvation therapy | Nature Nanotechnology"
[3]: https://pubs.acs.org/doi/abs/10.1021/nl052396o?src=recsys&utm_source=chatgpt.com "Determining the Size and Shape Dependence of Gold Nanoparticle Uptake into Mammalian Cells | Nano Letters"
[4]: https://www.nature.com/articles/s41467-025-58606-8?utm_source=chatgpt.com "Data splitting to avoid information leakage with DataSAIL | Nature Communications"
[5]: https://proceedings.mlr.press/v32/gardner14.html?utm_source=chatgpt.com "Bayesian Optimization with Inequality Constraints"
[6]: https://proceedings.neurips.cc/paper/2021/hash/11704817e347269b7254e744b5e22dac-Abstract.html?utm_source=chatgpt.com "Parallel Bayesian Optimization of Multiple Noisy Objectives with Expected Hypervolume Improvement"
[7]: https://academic.oup.com/bioinformatics/article/35/14/i464/5529233?utm_source=chatgpt.com "Enhancing the drug discovery process: Bayesian inference for the analysis and comparison of dose–response experiments | Bioinformatics | Oxford Academic"
[8]: https://pypi.org/project/MultiBgolearn/?utm_source=chatgpt.com "MultiBgolearn · PyPI"
[9]: https://proceedings.neurips.cc/paper/2020/hash/f5b1b89d98b7286673128a5fb112cb9a-Abstract.html?utm_source=chatgpt.com "BoTorch: A Framework for Efficient Monte-Carlo Bayesian Optimization"
[10]: https://arxiv.org/abs/2106.01309?utm_source=chatgpt.com "Benchmarking the Performance of Bayesian Optimization across Multiple Experimental Materials Science Domains"
[11]: https://www.sciencedirect.com/science/article/pii/S1359645422005146?utm_source=chatgpt.com "Multi-objective materials bayesian optimization with active learning of design constraints: Design of ductile refractory multi-principal-element alloys - ScienceDirect"
[12]: https://academic.oup.com/biostatistics/article/23/2/643/6071379?utm_source=chatgpt.com "Dose–response modeling in high-throughput cancer drug screenings: an end-to-end approach | Biostatistics | Oxford Academic"
